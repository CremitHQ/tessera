# `nebula path`
Manage the path where secrets are stored.

## Subcommands

### `nebula path create`

The `nebula path create` command creates a new path for storing secrets.

#### Flags
- `--path <path>`: The path to create.
