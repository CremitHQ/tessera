apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nebula-backbone.fullname" . }}-test-connection"
  labels:
    {{- include "nebula-backbone.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nebula-backbone.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
