apiVersion: v1
kind: Service
metadata:
  name: {{ include "nebula-backbone.fullname" . }}
  labels:
    {{- include "nebula-backbone.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nebula-backbone.selectorLabels" . | nindent 4 }}
