apiVersion: v1
kind: Service
metadata:
  name: {{ include "nebula-authorization.fullname" . }}
  labels:
    {{- include "nebula-authorization.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nebula-authorization.selectorLabels" . | nindent 4 }}
