apiVersion: v1
kind: Service
metadata:
  name: {{ include "nebula-authority.fullname" . }}
  labels:
    {{- include "nebula-authority.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nebula-authority.selectorLabels" . | nindent 4 }}
